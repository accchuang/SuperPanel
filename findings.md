# Findings

- 2026-09-24 行情终端数据可信与联动：现有 `/market/terminal` 含 `fetched_at/cache_age_seconds/data_mode`，但读取旧快照时未将快照写入时的 `LIVE` 降级；分钟 K 线一律标为已收盘；报价缺失时 `quote_time` 回退到最后 K 线时间，可能误认为真实报价时刻。
- 现有全市场趋势快照刻意排除化工、保留橡胶；CTA 最近报告按合约查询，未配置合约返回 404。因此联动卡需明确区分“未覆盖”与负面信号，且不能用不同实际合约的趋势结果直接当作本合约结论。
- 2026-09-24 浏览器联动实测：甲醇 MA2610 显示趋势未覆盖/CTA 未配置；切到棕榈油 P2701 显示同合约趋势状态、K 线形成中与 CTA 最近报告。该 CTA 报告生成于 9/8，但输入快照截至 9/3，若只显示生成时间会误读为当前结论，现明确显示“历史校验 · 数据截至 2026-09-03”。
- 信息卡放在图表既有右侧留白之内，主 K 线和副图继续占用原来的全高；窄屏可用顶部按钮收起信息卡。
- 交易蓝图三字段按具体合约代码保存在浏览器本地：方向、等什么、错在哪里。默认“观察”，绝不从趋势或 CTA 研究状态推导交易条件；数据凭证默认折叠以使蓝图、量仓、趋势与 CTA 在 720px 首屏同现。

- 2026-09-23 扩展自选池：TqSdk `query_cont_quotes()` 实时查询的当前主力标的为 `CZCE.SR701`、`CZCE.CF701`、`DCE.m2701`、`DCE.a2611`、`DCE.b2611`、`DCE.v2701`、`CZCE.MA610`；界面规范代码对应 SR2701、CF2701、M2701、A2611、B2611、V2701、MA2610。该映射是当下查询结果，不保证未来自动换月。
- 现有独立行情 worker 遍历 `LIVE_INSTRUMENTS × TIMEFRAMES`，新增静态自选合约会自动进入 1m/5m/15m/30m/1h/1d/3d 快照；前端终端另有一份静态自选列表，需要同步更新。
- 更新采集并重启后，1d/3d/30m/1h/15m 五个终端周期都返回 12 个合约，最新价无缺失；新增七合约的 3d 历史为 78—120 根、日线均为 80 根，15m 全池最短 69 根。
- 页面实测自选列表显示 12 个合约及报价，甲醇 MA2610 的 3 日 K、MA20/MA60、成交量副图正常显示；全局菜单仍折叠。
- 2026-09-23 横轴根因：TqSdk 的日线/3日K bar 标记时间为交易日 `00:00:00`，并非盘中成交时间；前端原先统一转成 UTC 秒数，导致午夜参与横轴。修复为 `1d/3d` 使用 Lightweight Charts 的交易日字符串，`15m/30m/1h` 继续把 `09:30`、`22:00` 等真实时分秒转为北京时间 UTC 秒。
- 用户反馈凌晨 1 点后确认盘中仍偏 8 小时：Lightweight Charts 自带光标格式化器用 UTC 小时读取 timestamp，因此把真实 `09:30+08` epoch 传入会显示 `01:30`。盘中时间现在用 `Date.UTC(年月日, 时, 分, 秒)` 保存北京时间字段，给图表 formatter 正确显示；浏览器光标标签实测 `10:30`。

- 2026-09-23 本次行情终端优化：现有 `/market/live?timeframe=1d` 已批量提供五个合约的实时报价，但响应约 152 KB，因为附带全部 K 线；自选列表当前仅缓存被点击合约。可由现有日线快照投影出精简的批量报价，不增加 TqSdk 订阅。
- 合约、周期、副图目前都只是 React 内存状态；浏览器本地保存时需等客户端恢复完成再发首轮行情请求、再写回存储，并对失效的合约/周期值做默认回退。
- `/market/watchlist` 直接投影现有 1 日快照，五合约响应 737 字节（原整份日线批量快照约 152 KB）；不创建新 TqSdk 订阅。页面每 6 秒独立拉取批量报价，单合约 K 线请求仍只取当前所选合约。
- 页面实测：五个合约均显示最新价和涨跌幅；选择菜籽油、3 日 K、成交量后刷新，三项保持；菜单继续默认折叠。

- 当前项目是 Next.js + TypeScript 前端，FastAPI 提供 overview、leaderboard、trend、strength、summary。
- 本次需求限定先做净持仓，因此优先改首页展示层，尽量不改变后端 API 契约。
- `strength` 原先只返回多头总持仓、空头总持仓和净值；现已增加按机构净持仓正负拆分的 `net_long`、`net_short`。
- 首页现已移除主表格，改为机构净持仓双向柱状图、今日净变化双向柱状图、净多/净空聚合面积图、Top机构折线图。
- 2026-09-04 的 JM、Y、OI 真实验证数据已写入 `data/real_positions_validation.csv`，每个品种 20 行，共 60 行。
- JM/Y 的大商所数据使用东方财富公开 `RPT_FUTU_DAILYPOSITION` fallback，按品种聚合全部合约的会员持仓后取 Top20；OI 仍使用 AKShare 郑商所接口。
- 真实数据已按品种替换导入 `data/dev.db`，其他品种数据保留；JM overview 和 Y strength API 已返回正常结果。
- 检查 RB、JM、Y、OI 的 leaderboard 均为 20 条且 broker 字段完整；席位名缺失来自 Recharts Y 轴默认抽样，已改为 `interval={0}` 并扩大标签列宽。
- CTA Beta 约束：仅研究与人工确认，不接入下单；趋势、结构、入场、风险使用独立评分，并由 Gate 决定状态，不能简单平均。
- 当前数据表只有按日品种/席位的持仓结构；CTA 所需 1H、30min、真实合约、交割与流动性数据需要新增独立市场快照契约和 Beta 样本适配层。
- `/live-market` 当前只从日 K 最后两行拼出一行报价，没有价格序列、7 根 OI 变化或周期参数；快照写在 `/tmp`，重启后无法可靠用于收盘回看。
- 现有前端已依赖 Recharts，可直接在表格单元格内绘制折线与正负柱，无需新增图表依赖。
- 新行情设计采用全局周期 `1m/5m/15m/30m/1h/1d`；每个品种返回区间价格折线点和恰好最近 7 根有效 K 线的 OI 增减，实时价改用 TqSdk quote 而不是 K 线 close。
- 静态回看使用项目 `data/live_market` 下的原子 JSON 快照；API 在休市或 TqSdk 暂不可用时继续返回最后成功快照，并明确标记静态状态。
- 后端 worker 同时维护 6 个周期、每品种 60 个价格点；OI 变化由相邻 K 线 `close_oi` 相减后截取最近 7 根，正负值分别用于向上/向下柱。
- TqSdk 在收盘后可能没有新 tick，首次无限期 `wait_update()` 会让静态快照永远不落盘；worker 必须为首次等待设置 deadline，并在超时后照常持久化已加载历史。
- “最近 3 个交易日”不能继续使用固定 60 根：1 分钟周期需要覆盖约 1,000 根；价格点应在后端按交易日裁剪，20:00 后夜盘归入下一工作日，OI 仍独立保留最近 7 根。
- 各周期订阅长度改为 `1m:1200 / 5m:300 / 15m:120 / 30m:72 / 1h:48 / 1d:10`，返回前再按最近 3 个交易日筛选，避免不同周期代表不同观察时长。
- 用户希望价格窗口扩大到 7 个交易日，并避免 OI 视觉像 K 线；OI 改用零轴上的连线加蓝白圆点，纵向位置表达增减幅度，悬浮继续展示时间、OI 与变化值。
- 七日窗口的订阅长度扩大为 `1m:2800 / 5m:600 / 15m:240 / 30m:120 / 1h:72 / 1d:14`；返回层默认保留最近 7 个夜盘归属后的交易日。
- 用户最终选择最近 5 个交易日作为价格折线窗口；OI 变化轨迹和最近 7 根 OI 数据保持不变。
- 趋势跟踪应基于最近 3 根已收盘日 K，而不是盘中尚未走完的当日日 K；否则成交量和持仓量天然偏低，会产生系统性误判。
- 现有行情快照只有价格序列与近 7 根 OI，没有逐日 OHLC、成交量和持仓量。趋势模块需要先扩展统一 Bar 数据契约，再在独立服务中计算三日方向、运行稳定度、成交量增长和持仓量增长。
- Beta 筛选保持三类互相可解释的结果：量仓共振、稳步上行、稳步下行。它们是研究信号，不直接映射为买卖建议。
- TqSdk 当前返回 88 个主力合约，其中 8 个来自中金所；按“国内商品主力”口径应先排除金融期货，再对 80 个商品品种应用化工黑名单。
- 趋势扫描应使用独立的日线 worker，避免把约 60 个主力合约乘以 6 个周期后拖慢原有实时自选行情。原有 5 合约实时页继续独立维护。
- 化工过滤采用显式品种代码配置；天然橡胶 `RU`、20号胶 `NR`、合成橡胶 `BR` 作为例外优先保留。原油 `SC` 归能源，不按化工排除。
- CTA 历史验证不能复用最新数据：回放评分必须以所选交易日为截点，日线和席位 Feature 均只读取该日及之前的记录；后续 1/3/5 日表现只能在评分完成后计算。
- 当前回放属于日线收盘事件研究：用 `READY + LONG/SHORT` 才计算方向收益；其他机器状态只展示标的后续涨跌，标记为未触发。手续费、滑点、仓位和移仓尚未纳入。
- 趋势规则的历史回测必须使用 `KQ.m@交易所.品种` 主连连续序列；用今天的真实主力合约直接回溯会混入非主力时期的流动性和移仓偏差。天勤主连依据持仓量和成交量选定，并在下一交易日切换。
- 当前趋势分的可解释权重为：价格结构 60（连续收盘方向、两段涨跌均衡、三日波幅），成交量 20，持仓量 20；后两项分别以三日上行或最新值高于此前五日均值确认。
- 首轮 61 品种约 120 日主连样本的汇总未证明量仓确认具备正期望或稳定增量，因此不应据此继续调阈值拟合历史；优先扩充样本、做分板块/滚动窗口检验，并把量仓条件标准化为相对异常度。
- 三日 K 线适合描述短期状态而非定义主趋势：在 20 日均线位置与斜率仍保持上行时，连续三日走弱应标记为上升趋势中的回踩，不能反转为下行确认。实时快照必须保存 26 根日线，才可在盘中保留 25 根已收盘日线用于 MA20 与五日斜率。
- 本次异常扫描可直接复用 `data/live_market/trend-history-1d.json`：该快照覆盖已过滤化工品种的国内商品主连，并保存约120根已收盘日K及成交量、收盘持仓量。
- 主连在换月日会把不同真实合约的成交量和持仓量拼接，菜油、鸡蛋、氧化铝出现36%—117%的单日持仓跳变，不能直接解释为新增资金；异常扫描应改用当前实际主力合约的独立日线，主连仅用于价格趋势。
- 2026-09-09 实际主力强异常：AO2701（成交量2.68倍、OI+19.24%、增仓下跌）、AP2701（成交量2.70倍、OI+14.53%、增仓上涨）、OI2701（成交量15日新高、OI+3.70%、高换手微涨）、I2701（OI+3.65%、增仓下跌）。
- 2026-09-08 实际主力强异常：A2611（成交量15日新高、OI+8.54%、增仓上涨）、SR2701（OI+4.94%、增仓上涨）、CU2610（OI+7.22%、增仓上涨）、JM2701（涨3.81%、OI+11.59%）、JD2611（成交量1.88倍、增仓下跌）。
- 两日连续结构中，OI2701连续增仓但第二日价格涨幅收敛，JD2611连续增仓而两日价格基本回到原位，均属于高分歧而非直接追价信号。
- 2026-09-23 行情时段底纹：盘中 K 线按 20:00—次日 03:00 归夜盘、08:00—16:00 归日盘；夜盘跨零点按次日交易日分组。Lightweight Charts pane primitive 的背景绘制同步铺在 K 线和 OI/成交量副图，日线与 3 日 K 不着色。浏览器 30 分钟图确认时段淡色底纹和 K 线同时可见。
- 根据用户反馈将日夜盘底纹 alpha 从 0.24 提至 0.32，仍以白色画布合成浅色，图表颜色图例复用同一色值；浏览器确认 K 线、均线与成交量清晰，日夜盘色差增强。
- 2026-09-23 图表极值标注：对最近 45 根 `is_closed=true` 的 K 线独立排名成交量与实体幅度 `abs(close-open)`，各标前三；当前形成中的 K 线不参加统计。并列按较新的 K 线优先，同一根可同时获得量能/实体标记。
- 2026-09-23 用户偏好 K 线标记不带文字：量能与实体极值均改为小圆点，用蓝灰/暖金色及上下位置区分；保留图例和悬停详情。新增纯数据标记测试，前端 12 项测试及生产构建通过，浏览器确认 K 线上无文字标签。
- 2026-09-24 终端操作效率：左侧自选按板块分组并可折叠；⌘K/Ctrl+K 打开名称、代码、交易所和板块搜索，方向键选择、回车切换；同板块比较只展示现有批量报价真实提供的最新价、涨跌幅和报价时间。
- 事件雷达仅评估已收盘K线：ATR通道(EMA45高低价 ± EMA60真实波幅×1.5)突破；最近成交量或绝对 OI 变化达到前20根中位数的2倍；趋势研究快照分类发生变化。每类按K线/状态标识去重，样本不足不触发，不将事件解释为买卖信号。
- 本地布局保存合约、周期、副图、ATR通道和右侧信息面板状态，最多保留12个；启动加载时先读取再写入，避免初始空状态覆盖已有布局。
- 浏览器实测 Ctrl+K 过滤 BR 并回车切换、橡胶三兄弟报价对比、保存“盘中观察”布局后切到15分钟再载入恢复30分钟；已恢复为 P2701/30分钟/成交量。前端生产构建通过。
- 独立行情终端复用现有 TqSdk worker；`build_market_quote` 新增 `bars`（OHLC、close_oi、相邻 OI 变化），`/market/terminal` 仅返回选中合约；前端采用 Lightweight Charts 5.2.1 的 CandlestickSeries + HistogramSeries。
- 日线历史已从 10 根扩为 90 根订阅、返回最近 80 个交易日，终端用这些日线计算 MA20/MA60；其他周期仍保留原五日价格窗口，避免把高频快照无界放大。
- 30 分钟/1 小时当时仍被 5 交易日窗口裁剪，接口仅返回 53/31 根（订阅上限 100/60），不足以计算 MA60。现将订阅扩至 900/500 根、保留 80 交易日；30m/1h 实际返回 900/500 根，分别覆盖 2026-06-08 至 2026-09-23、2026-06-12 至 2026-09-23。其他分钟周期五日窗口已有至少 60 根，继续保留以控制快照体量。
- 行情终端 OI 副图原本有数据，但固定 430px 主图、148px 副图叠加展开的全局导航把副图挤出窄屏首屏。终端头部现压缩，窄屏可折叠全局导航，主/副图高度按视口适配；浏览器首屏已确认完整显示 K 线与 OI。
- 2026-09-24 信息卡重叠根因：父容器中的图表按全宽渲染，信息卡再以 `position:absolute` 叠放于右侧；现改为 flex 两列，为信息卡固定预留 252px，图表与价格轴不再被遮挡。生产构建及浏览器截图已确认两列布局。
- 2026-09-24 市场全景使用独立的 `query_cont_quotes()` 全商品主力列表，不复用排除化工的 61 品种趋势跟踪。实测 TqSdk 返回 80 个商品品种、10 个板块，其中化工 19 个；最新交易日有价格的 74 个进入前台统计，6 个停滞或无价品种不参与当前方向判断。
- 板块近 5 日方向使用同板块各具体主力合约的 5 日价格涨跌幅中位数，并展示上涨品种数；这是描述性聚合，不是可交易板块指数。盘中最后一点用实时报价，历史点只取已完成日线；前台仅展示价格、涨跌和简短走势线。
